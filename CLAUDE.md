@AGENTS.md

## Claude Code specifics

- Полная спека и статус: `PLAN.md` — читать целиком при возвращении к проекту.
- Исходный план и вся история решений (v1→v4, три ресёрча, две проверки Perplexity):
  `~/assistant-core/plans/mcp-research-server.md`. PLAN.md — рабочая выжимка, тот файл — архив.
- Читаемый исходник соседа: `sweetcornna/free-search-mcp` (MIT). Его кеш —
  `~/.cache/search-mcp/cache.sqlite`, таблица `pages(url, title, content, fetched)`.
  Внимание: в `title` склеен служебный префикс `\x01META\x01{...json...}` — срезать при чтении.
- **Почему решения именно такие** — `docs/research/` (по файлу на вопрос, с источниками)
  и спека `docs/superpowers/specs/2026-07-26-stage-2-briefs-and-citations-design.md`.
  Прежде чем пересматривать решение — прочитать соответствующий ресёрч, а не заново гуглить.
- Правка `~/.claude/agents/research.md` (этап 2) — только с явного разрешения Петра из консоли.

## Session Log

### 2026-07-26
- Сделано: проект создан с нуля. Этап 0 (free-search-mcp поставлен и зарегистрирован как `search`),
  этап 1 (состояние + фрагменты), этап 2 (брифы, цитаты, метрики) — 12 коммитов, 72 теста.
- Решения: жёсткая проверка цитат в коде; бриф приходит структурой, собирается в два шага
  (сначала проза, потом упаковка); фрагменты по схеме W3C со снапшотом; `research` не тронут,
  добытчик вынесен в отдельного агента `research-fetch`.
- Наблюдения: смоук-тест поймал баг с потоками FastMCP до первого использования; на живом кеше
  free-search-mcp в `title` есть служебный `\x01META\x01{json}\x01` — фикстура без хвостового
  `\x01` пропустила баг, поймал реальный прогон. Замер экономии контекста: 93%.
- Открыто: пуш (репо на GitHub не найден, 404); MCP-слой не проверен — тулы появятся после
  рестарта Claude; скилл и `research-fetch` на живой задаче не обкатаны.
